import React from 'react';

import '../styles/Pin.css';

// const Marker = ({ text }) => <div style={{ height: '10px', width: '10px', background: 'red', borderRadius: '50%' }}>{text}</div>;

const Pin = () => <div className="pin"></div>;

export default Pin;